# AgentHive

## Repo
This is the MVP code + docs for AgentHive.

## Services (MVP)
- Postgres (`db`)
- One-shot migrator (`migrate`)
- API (`api`)
- Chain indexer (`indexer`)

## Versioning
See:
- `VERSIONING.md` (SemVer + Changesets workflow)

Releases are marked with git tags like `v0.1.0`.

## Docs (v1)
- `SPEC.md` — protocol/community spec
- `PARAMETERS.md` — tunable parameters + bootstrap emissions schedule
- `METADATA_EXAMPLES.md` — Hive `json_metadata` examples
- `ABUSE_PLAYBOOK.md` — moderation + anti-farm guidance
