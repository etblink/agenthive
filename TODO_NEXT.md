# AgentHive — Next TODOs (ordered)

1) Lock runtime/deploy target ✅
- v1 default: single VPS, docker-compose

2) Lock Hive ingestion approach ✅
- v1 default: managed Hive RPC endpoints

3) Implement DB migrations + seed config
- curator whitelist + denylist tables or config loader

4) Stand up indexer skeleton
- ingest blocks → filter app/tag → write content

5) Stand up Next.js UI skeleton
- feed + post detail + compose

6) Add payout runner scaffolding (dry-run first)
- daily participation; weekly curated
